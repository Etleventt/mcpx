# SubDesk 邀请内测安装与配对

本文供用户与其本机 Agent 使用。只下载 `https://github.com/Etleventt/mcpx/releases` 中明确标注 SubDesk 的版本；平台地址和邀请码由运营方单独提供。无需朋友购买或部署服务器。

## 安装

先检测系统与 CPU 架构。Apple 芯片 Mac 选择 macos-arm64，64 位 Intel/AMD Windows 选择 windows-amd64；只有发布页实际提供相应资产时才支持其他平台。普通用户不要下载 server 包。

下载 ZIP 及同版本 SHA256SUMS.txt，先验证 ZIP 摘要，再完整解压。包内安装器会校验清单。Mac 使用 Install.command；Windows 使用 Install.cmd，按包内 INSTALL_AGENT.md 完成当前用户级安装。已有应用和配置应先保留备份，不能以安装之名删除项目、缓存或其他 Agent 的设置。

当前内测包没有正式 Developer ID 公证／Authenticode 签名。校验和不等于开发者签名；不要关闭 Gatekeeper、SmartScreen、防病毒或系统防火墙。被系统拦截时先核对来源，不能让 Agent 隐蔽绕过保护。

## 网站登录不等于设备已连接

1. 用运营方给的邀请码在网站注册并登录。平台密码只用于网站，不要告诉 Agent。
2. 打开 SubDesk 客户端的登录与配对页面，填平台 HTTPS 地址和设备名称，并自行设置至少 12 字节的独立设备访问口令。不要复用网站密码，也不要把口令写进聊天或命令行参数。
3. 客户端显示一次性配对码并打开浏览器。可点击复制配对码；在网页核对设备名称和代码后批准。浏览器单独显示登录成功，并不代表客户端已收到配对结果。
4. 等客户端明确显示绑定完成，再检查 Runtime 与 Agent 在线状态。只添加本人指定的非敏感工作目录，权限默认保持 standard。
5. 从网站设备卡片或客户端复制完整 MCP 地址，形如 `https://your-platform.example/d/设备ID/mcp`，不要只填网站域名，也不要填 `/console/login`。
6. 用户在支持自定义 MCP 的客户端添加此地址并亲自完成网站登录和独立设备口令授权。ChatGPT 自定义应用是否可用，取决于用户账号和工作区权限。

Device ID 与 Device Token 由配对流程自动管理，不需要复制给朋友或手填。设备离线时即便网页可打开，MCP 也无法执行；先检查客户端的绑定与在线状态，不要反复创建插件或关闭鉴权。

## 旧平台或旧客户端迁移

平台换域名后，使用运营方提供的新 HTTPS 地址登录。账号、设备额度与已有设备是否迁移以该次发布说明为准；不要擅自新建管理员或清空数据库。

客户端如发现已有不同的 Runtime 公开地址，会阻止静默覆盖。只有用户明确选择迁移时才备份并替换；旧 ChatGPT 连接可能要重新授权。保留旧插件作为回退，新建连接不会自动把正在进行的对话切到新线路。

配对码过期或配对失败时，按客户端提示重试。若网页里出现已批准但未上线的设备，应先确认是否有可恢复的本机配对记录；不能凭设备名称猜测他人设备并进行授权。设备不再使用时，从本人设备列表撤销。

## 安全与隐私边界

平台账号管理准入与设备归属，本地 Runtime 独立控制工作目录和执行权限。standard 会对需确认的动作请求批准；trusted 只应由设备所有者明确开启，硬拒绝仍然生效。

当前 Hosted Hub 在服务器终止 TLS，**不是端到端加密，也不保证平台运营方无法读取 OAuth/MCP 内容或重放有效授权**。配对时独立设备口令仅交给本机；之后浏览器的设备授权经过中转时，运营方技术上仍可能接触到相关内容。请勿用于生产密钥、公司机密或敏感隐私。

本机凭据由文件权限或 Windows DACL 保护，不把它宣传成 Keychain/DPAPI 加密。不要向公开仓库、聊天、截图或诊断日志复制设备凭据、密码、初始化票据或完整配置。

安装 Agent 最后只需报告：实际安装版本、安装路径、平台地址、设备名称、是否绑定、Runtime/Agent 是否在线、允许工作目录和权限模式。没有完成的登录、配对或真实 ChatGPT 验收必须明确说明，不能用构建成功代替。
